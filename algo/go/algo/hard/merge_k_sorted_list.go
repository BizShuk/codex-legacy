package hard

// Question: Merge k-sorted list(Linked List)

// Use heap to put current element to heap and pop one by one
// When pop, add another one from the same list

// How to know what list of the poped element?
// Ans: It's linked list, so when poped, the node knows the next node.
